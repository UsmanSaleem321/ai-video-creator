# Core deep learning
# diffusers>=0.30.0 already provides StableVideoDiffusionPipeline (image-to-video)
# and IPAdapterMixin (character-consistency conditioning) used by the cinematic
# pipeline (modules/video_generator.py, modules/character_consistency.py) -
# no separate package needed for either feature.
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.38.0
diffusers>=0.30.0
accelerate>=0.27.0
bitsandbytes>=0.42.0
peft>=0.9.0
git+https://github.com/THUDM/CogVideo.git

# Memory-efficient attention. Colab's default runtime does NOT have this
# preinstalled; it meaningfully lowers peak VRAM for both the SD1.5+IP-Adapter
# keyframe stage and the Stable Video Diffusion stage on a T4. Both
# image_generator.py and video_generator.py already guard xformers usage with
# try/except, so the pipeline still runs (just slower / more VRAM-hungry) if
# this wheel fails to install for a given Colab CUDA/torch build.
xformers>=0.0.23

# Audio
suno-bark @ git+https://github.com/suno-ai/bark.git
soundfile>=0.12.1
pydub>=0.25.1

# Video
moviepy>=1.0.3
imageio-ffmpeg>=0.4.9  # ffmpeg binary for moviepy; Colab's system ffmpeg is not always picked up by pip-installed moviepy
decord>=0.6.0

# Hugging Face / model management
huggingface_hub>=0.20.0
einops>=0.7.0
protobuf>=3.20.0
safetensors>=0.4.0

# Utilities
numpy>=1.24.0
Pillow>=10.0.0
opencv-python>=4.9.0
tqdm>=4.66.0
python-dotenv>=1.0.0
